os: mac
app: Atom
-

spring <user.number>:
    key(ctrl-g)
    key(number)
    key(enter)

action(app.tab_open):
  key(cmd-n)

don't save: key(cmd-d)

trundle: key(cmd-/)

show menu: key(cmd-shift-p)

(pane right) | (right pane):
    key(cmd-k)
    key(cmd-right)

(pane left) | (left pane):
    key(cmd-k)
    key(cmd-left)
